#include <stdio.h>
int main()
{
    int n;
    scanf("%d",&n);
    int k=n;
    while(k>=1){
        int a=n;
        while(a>=k){
            printf("%d ",a);
            a--;
        }
        k--;
        printf("\n");
    }
}
