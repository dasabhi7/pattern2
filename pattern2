#include <stdio.h>
int main()
{
    int n;
    scanf("%d",&n);
    int k=1;
    while(k<=n){
        int a=n;
        while(a>=k){
            printf("%d ",a);
            a--;
        }
        k++;
        printf("\n");
    }
}
